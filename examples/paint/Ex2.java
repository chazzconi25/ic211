import java.awt.*;
import javax.swing.*;

import javax.imageio.*;
import javax.image.*;


public class Ex2 extends JComponent {
    public DrawArea() {
        super();
        setPreferredSize(new Dimension(400, 400));
    }

    protected void paintComponent(Graphics g) {
        super.paintComponent(g);

        Graphics2D g2 = (Graphics2D)
    }
}
